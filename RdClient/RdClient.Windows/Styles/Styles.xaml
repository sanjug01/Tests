<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Styles"
    xmlns:controls="using:RdClient.Controls"
    xmlns:c="using:RdClient.Converters"
    >
    
    <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Default">
            <SolidColorBrush x:Key="ConnectionCenterBarBackgroundBrush" Color="{ThemeResource SystemChromeHighColor}" />
            <SolidColorBrush x:Key="ConnectionCenterScrollViewBackgroundBrush" Color="{ThemeResource SystemAltHighColor}" />
        </ResourceDictionary>        
        <ResourceDictionary x:Key="Light">
            <SolidColorBrush x:Key="ConnectionCenterBarBackgroundBrush" Color="{ThemeResource SystemChromeHighColor}" />
            <SolidColorBrush x:Key="ConnectionCenterScrollViewBackgroundBrush" Color="{ThemeResource SystemAltHighColor}" />
        </ResourceDictionary>
        <ResourceDictionary x:Key="HighContrast">
            <SolidColorBrush x:Key="ConnectionCenterBarBackgroundBrush" Color="{ThemeResource SystemColorWindowColor}" />
            <SolidColorBrush x:Key="ConnectionCenterScrollViewBackgroundBrush" Color="{ThemeResource SystemColorWindowColor}" />
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <FontFamily x:Key="IconFont">ms-appx:///Assets/SegMDL2.1.12.ttf#Segoe MDL2 Assets</FontFamily>
    <x:String x:Key="AddIcon">&#xE710;</x:String>
    <x:String x:Key="MultiSelectIcon">&#xE762;</x:String>
    <x:String x:Key="MoreIcon">&#xE712;</x:String>
    <x:String x:Key="SearchIcon">&#xE721;</x:String>
    <x:String x:Key="SettingsIcon">&#xE713;</x:String>
    <x:String x:Key="EditIcon">&#xE70F;</x:String>
    <x:String x:Key="DeleteIcon">&#xE74D;</x:String>
    <x:String x:Key="BackIcon">&#xE112;</x:String>
    <x:String x:Key="ZoomInIcon">&#xE1A3;</x:String>
    <x:String x:Key="ZoomOutIcon">&#xE1A4;</x:String>
    <x:String x:Key="UpIcon">&#xE110;</x:String>
    <x:String x:Key="DownIcon">&#xE1FD;</x:String>
    <x:String x:Key="LeftIcon">&#xE112;</x:String>
    <x:String x:Key="RightIcon">&#xE111;</x:String>
    <x:String x:Key="MoreDetailsIcon">&#xE099;</x:String>
    <x:String x:Key="LessDetailsIcon">&#xE098;</x:String>
    <x:String x:Key="KeyboardIcon">&#xE765;</x:String>
    <x:String x:Key="DisconnectIcon">&#xE711;</x:String>

    <Style x:Key="InvisibleButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="Transparent" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                                       Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightAltListAccentLowBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                                       Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightAltListAccentHighBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Margin="{TemplateBinding Margin}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 
    ======================================================================================
    Connection Center styles 
    ======================================================================================
    -->            
    <SolidColorBrush x:Key="ConnectionCenterDesktopPlaceholderIconColor" Color="#9D9D9D" />
    <SolidColorBrush x:Key="ConnectionCenterHostnameBackgroundColor" Color="#59000000" />    
    <SolidColorBrush x:Key="ConnectionCenterScrollViewBackgroundBrush" Color="#000000" />
    <SolidColorBrush x:Key="ConnectionCenterBarButtonLightNormalForegroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ConnectionCenterBarButtonLightPressedForegroundBrush" Color="#FFFFFF"/>
    <GridLength x:Key="ConnectionCenterMargin">68</GridLength>   
    <x:Double x:Key="ConnectionCenterTitleBarHeight">68</x:Double>
    <x:Double x:Key="ConnectionCenterButtonWidth">68</x:Double>
    <GridLength x:Key="ListHeaderHeight">34</GridLength>

    <Style x:Key="ConnectionCenterBarButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="{StaticResource ConnectionCenterButtonWidth}" />
        <Setter Property="Height" Value="{StaticResource ConnectionCenterTitleBarHeight}" />
        <Setter Property="Background" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="ConnectionCenterBarNarrowButtonStyle" TargetType="Button" BasedOn="{StaticResource ConnectionCenterBarButtonStyle}">
        <Setter Property="Width" Value="48" />    
    </Style>
    
    <SolidColorBrush x:Key="ConnectionCenterSelectorUnselectedBrush" Color="#999999" />
    <SolidColorBrush x:Key="ConnectionCenterPointerOverBackgroundBrush" Color="#6B6B6B" />    
    <Style x:Key="ConnectionCenterSectionSelectorItemStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="{StaticResource ConnectionCenterTitleBarHeight}" />
        <Setter Property="Background" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
        <Setter Property="TabNavigation" Value="Local" />
        <Setter Property="Padding" Value="34,0,34,0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="26.667"/>
        <Setter Property="FontWeight" Value="Light"/> 
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="LayoutRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="InnerGrid"
                                                                    Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ConnectionCenterPointerOverBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed" >
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="InnerGrid"
                                                                    Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>                                        
                                    </Storyboard>                                    
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ConnectionCenterSelectorUnselectedBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>                                    
                                </VisualState>
                                <VisualState x:Name="Selected">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="SelectedUnfocused" />
                                <VisualState x:Name="SelectedDisabled" />
                                <VisualState x:Name="SelectedPointerOver" />
                                <VisualState x:Name="SelectedPressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="InnerGrid"
                            Background="Transparent">
                            <Rectangle x:Name="PressedBackground"
                                    Fill="{StaticResource SettingsBackgroundBrush}"
                                    Opacity="0" />
                            <ContentPresenter x:Name="ContentPresenter"
                                            Content="{TemplateBinding Content}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ConnectionCenterSectionSelectorStyle" TargetType="ListBox">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="Transparent" />
        <Setter Property="Padding" Value="34,0,0,0" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource ConnectionCenterSectionSelectorItemStyle}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border x:Name="LayoutRoot" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Focused" />
                                <VisualState x:Name="Unfocused"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ScrollViewer x:Name="ScrollViewer"
                                  Padding="{TemplateBinding Padding}"
                                  TabNavigation="{TemplateBinding TabNavigation}"
                                  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                  IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
                                  IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
                                  ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}"
                                  IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                  BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
                                  AutomationProperties.AccessibilityView="Raw">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- 
    ======================================================================================
    BackButton styles
    ======================================================================================
    -->
    <x:Double x:Key="BackButtonHeight">30</x:Double>
    <SolidColorBrush x:Key="BackButtonBackgroundBrush" Color="#008FF1" />

    <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource ConnectionCenterBarButtonStyle}">
        <Setter Property="Height" Value="{StaticResource BackButtonHeight}" />
        <Setter Property="Width" Value="68" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{StaticResource BackButtonBackgroundBrush}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Content" Value="{StaticResource BackIcon}" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <!-- 
    ======================================================================================
    Connection Bar styles 
    ======================================================================================
    -->
    <Style x:Key="ConnectionBarTitleButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="60" />
        <Setter Property="Background" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="18" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="-2,0,-2,0"/>
        <Setter Property="Opacity" Value="0.6"/>
    </Style>

    <Style x:Key="ConnectionBarButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="48" />
        <Setter Property="Height" Value="60" />
        <Setter Property="Background" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />        
        <Setter Property="Margin" Value="-2,0,-2,0"/>
        <Setter Property="Opacity" Value="0.6"/>
    </Style>    
    
    <Style x:Key="ConnectionBarToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Width" Value="48" />
        <Setter Property="Height" Value="60" />
        <Setter Property="Background" Value="{ThemeResource ConnectionCenterBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="-2,0,-2,0" />
        <Setter Property="Opacity" Value="0.6"/>
    </Style>

    <!-- 
    ======================================================================================
    Settings styles 
    ======================================================================================
    -->
    <SolidColorBrush x:Key="SettingsNavBarBackgroundBrush" Color="#1A1A1A" />
    <SolidColorBrush x:Key="SettingsSelectedItemBackgroundBrush" Color="#454545" />
    <SolidColorBrush x:Key="SettingsBackgroundBrush" Color="#FFFFFF" />    
    <GridLength x:Key="SettingsTopPadding">120</GridLength>
    <GridLength x:Key="SettingsMargin">72</GridLength>
    <Thickness x:Key="SettingsMarginPadding">72,0</Thickness>
    <GridLength x:Key="SettingsItemHeight">48</GridLength>
    <x:Double x:Key="SettingsItemHeightDouble">48</x:Double>
    <GridLength x:Key="SettingsItemColumnWidth">428</GridLength>
    <x:Double x:Key="SettingsItemColumnWidthDouble">428</x:Double>
    <GridLength x:Key="SettingsItemMargin">20</GridLength>

    <Style x:Key="SettingsListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Height" Value="{StaticResource SettingsItemHeightDouble}" />
        <Setter Property="Background" Value="{StaticResource SettingsNavBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SettingsBackgroundBrush}" />
        <Setter Property="TabNavigation" Value="Local" />
        <Setter Property="Padding" Value="{StaticResource SettingsMarginPadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="IsSelected" Value="{Binding Path=ShowExtended, Mode=TwoWay}" />
        <Setter Property="Template">           
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="LayoutRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot"
                                                                    Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource BackButtonBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PressedBackground"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="1"
                                                        Duration="0" />
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SettingsNavBarBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected" />
                                <VisualState x:Name="Selected">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="InnerGrid"
                                                                    Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SettingsSelectedItemBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="SelectedUnfocused">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="InnerGrid"
                                                                    Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SettingsSelectedItemBackgroundBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="SelectedDisabled" />
                                <VisualState x:Name="SelectedPointerOver" />
                                <VisualState x:Name="SelectedPressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="InnerGrid"
                            Background="Transparent">
                            <Rectangle x:Name="PressedBackground"
                                    Fill="{StaticResource SettingsBackgroundBrush}"
                                    Opacity="0" />
                            <ContentPresenter x:Name="ContentPresenter"
                                            Content="{TemplateBinding Content}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="SettingsAddUserButtonBackgroundBrush" Color="#B2B2B2" />
    <SolidColorBrush x:Key="SettingsAddUserButtonForegroundBrush" Color="#000000" />
    <Style x:Key="SettingsAddUserButtonStyle" TargetType="Button" BasedOn="{StaticResource ConnectionCenterBarButtonStyle}">
        <Setter Property="Height" Value="35" />
        <Setter Property="Width" Value="35" />
        <Setter Property="Background" Value="{StaticResource SettingsAddUserButtonBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SettingsAddUserButtonForegroundBrush}" />
    </Style>
    <SolidColorBrush x:Key="SettingsUserBulletBrush" Color="#3FA9F5" />
    <Style x:Key="SettingsUserBulletStyle" TargetType="Rectangle">
        <Setter Property="Fill" Value="{StaticResource SettingsUserBulletBrush}" />
        <Setter Property="Height" Value="35" />
        <Setter Property="Width" Value="35" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style x:Key="SettingsEditUserButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="32" />
        <Setter Property="Width" Value="120" />
        <Setter Property="Foreground" Value="{StaticResource ConnectionCenterBarButtonLightNormalForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource SettingsUserBulletBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>
    <Style x:Key="SettingsDeleteUserButtonStyle" TargetType="Button" BasedOn="{StaticResource SettingsEditUserButtonStyle}">
        <Setter Property="Background" Value="{StaticResource SettingsAddUserButtonBackgroundBrush}" />
    </Style>
    
    <DataTemplate x:Key="CredentialDataTemplate">
        <controls:CredentialControl />
    </DataTemplate>
    
    <DataTemplate x:Key="SelectedCredentialDataTemplate">
        <controls:ExtendedCredentialControl />
    </DataTemplate>
    
    <SolidColorBrush x:Key="CredentialSelectedColor" Color="#1A000000" />
    
    <Style x:Key="SettingsCredentialListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0" />
        <Setter Property="TabNavigation" Value="Local" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="LayoutRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="0">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PressedHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0.2"
                                                        Duration="0" />                                      
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PressedHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0.5"
                                                        Duration="0" />                                        
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ItemHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0"
                                                        Duration="0" />
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="ContentTemplate">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource CredentialDataTemplate}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Selected" />
                                <VisualState x:Name="SelectedUnfocused" />
                                <VisualState x:Name="SelectedDisabled" />
                                <VisualState x:Name="SelectedPointerOver" />
                                <VisualState x:Name="SelectedPressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="InnerGrid"
                            Background="Transparent">                         
                            <ContentPresenter x:Name="ContentPresenter"
                                            Content="{TemplateBinding Content}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            ContentTemplate="{StaticResource SelectedCredentialDataTemplate}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}" />
                            <Rectangle x:Name="ItemHighlight" IsHitTestVisible="False"
                                    Fill="{StaticResource CredentialSelectedColor}"
                                    Opacity="1" />
                            <Rectangle x:Name="PressedHighlight" IsHitTestVisible="False"
                                    Fill="{StaticResource BackButtonBackgroundBrush}"
                                    Opacity="0" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- gateways list box is similar to credentials list box-->

    <DataTemplate x:Key="GatewayDataTemplate">
        <controls:GatewayControl />
    </DataTemplate>

    <DataTemplate x:Key="SelectedGatewayDataTemplate">
        <controls:ExtendedGatewayControl />
    </DataTemplate>
    
    <Style x:Key="SettingsGatewayListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0" />
        <Setter Property="TabNavigation" Value="Local" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="LayoutRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="0">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PressedHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0.2"
                                                        Duration="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PressedHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0.5"
                                                        Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ItemHighlight"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0"
                                                        Duration="0" />
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter"
                                                                    Storyboard.TargetProperty="ContentTemplate">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource GatewayDataTemplate}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Selected" />
                                <VisualState x:Name="SelectedUnfocused" />
                                <VisualState x:Name="SelectedDisabled" />
                                <VisualState x:Name="SelectedPointerOver" />
                                <VisualState x:Name="SelectedPressed" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid x:Name="InnerGrid"
                            Background="Transparent">
                            <ContentPresenter x:Name="ContentPresenter"
                                            Content="{TemplateBinding Content}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            ContentTemplate="{StaticResource SelectedGatewayDataTemplate}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}" />
                            <Rectangle x:Name="ItemHighlight" IsHitTestVisible="False"
                                    Fill="{StaticResource CredentialSelectedColor}"
                                    Opacity="1" />
                            <Rectangle x:Name="PressedHighlight" IsHitTestVisible="False"
                                    Fill="{StaticResource BackButtonBackgroundBrush}"
                                    Opacity="0" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- 
    ======================================================================================
    Dialog styles 
    ======================================================================================
    -->    
    <SolidColorBrush x:Key="DialogBackgroundColor" Color="#FFFFFF" />
    <SolidColorBrush x:Key="DialogAcceptButtonBackgroundColor" Color="#0078D7" />
    <SolidColorBrush x:Key="DialogCancelButtonBackgroundColor" Color="#CCCCCC" />
    <SolidColorBrush x:Key="DialogButtonForegroundColor" Color="#FFFFFF" />

    <Style x:Key="DialogCancelButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="32" />
        <Setter Property="Width" Value="120" />
        <Setter Property="Foreground" Value="{StaticResource DialogButtonForegroundColor}" />
        <Setter Property="Background" Value="{StaticResource DialogCancelButtonBackgroundColor}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>
    <Style x:Key="DialogAcceptButtonStyle" TargetType="Button" BasedOn="{StaticResource SettingsEditUserButtonStyle}">
        <Setter Property="Background" Value="{StaticResource DialogAcceptButtonBackgroundColor}" />
    </Style>
   
    <Style x:Key="DialogDetailsButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="20" />
        <Setter Property="Width" Value="32" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="{StaticResource DialogAcceptButtonBackgroundColor}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="ModalBackground" TargetType="Border">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Opacity" Value="0.8" />
        <Setter Property="Background" Value="#dd000000" />
    </Style> 

    <Style x:Key="DialogBorder" TargetType="Border">
        <Setter Property="Width" Value="434" />
        <Setter Property="Background" Value="{StaticResource DialogBackgroundColor}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Margin" Value="186" />
        <Setter Property="Padding" Value="43,15,43,35" />        
    </Style>
    <!-- 
    ======================================================================================
    ======================================================================================
    -->
    <SolidColorBrush x:Key="rdBlackBrush" Color="Black" />
    <SolidColorBrush x:Key="rdGrayBrush" Color="Gray" />
    <SolidColorBrush x:Key="rdWhiteBrush" Color="White" />
    <SolidColorBrush x:Key="rdErrorBrush" Color="Red" />

    <Style x:Key="rdEntryFieldGridStyle" TargetType="Grid" >
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>

