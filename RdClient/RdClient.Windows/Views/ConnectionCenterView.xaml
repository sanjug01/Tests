<Page
    x:Class="RdClient.Views.ConnectionCenterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource AppViewModelLocator}, Path=ConnectionCenterViewModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Black" /> 
        <Grid Grid.Row="1" Background="Orange">
            <Button Command="{Binding AddDesktopCommand}" HorizontalAlignment="Right">Add Desktop(d)</Button>
        </Grid>
        <Grid Grid.Row="2" Background="White">
            <ItemsControl ItemsSource="{Binding DesktopViewModels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:DesktopThumbnailView />
                    </DataTemplate>                    
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</Page>
