<Page
    x:Class="RdClient.Views.SessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Views"
    xmlns:vm="using:RdClient.Shared.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    DataContext="{Binding SessionViewModel}">
    
    <Page.Resources>
        <vm:SessionViewModel x:Key="SessionViewModel"/>
    </Page.Resources>

    <Grid Background="green">
        <SwapChainPanel x:Name="SwapChainPanel"/>
        <Button
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right" Command="{Binding Source={StaticResource SessionViewModel}, Path=DisconnectCommand}">Disconnect</Button>
    </Grid>
</Page>
