<UserControl
    x:Class="RdClient.Views.DesktopThumbnailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:design="using:RdClient.DesignTime"
    mc:Ignorable="d"
    d:DesignHeight="256" Width="273">
    
    <d:UserControl.DataContext>
        <design:FakeDesktopViewModel />
    </d:UserControl.DataContext>

    <StackPanel>
        <Grid Background="{StaticResource ConnectionCenterDesktopPlaceholderIconColor}">
            <Grid Height="256" Width="Auto"> 
                <Grid.Background>
                    <ImageBrush ImageSource="ms-appx:///Assets/desktop_placeholder.png" Stretch="None" AlignmentX="Center" AlignmentY="Center"/>
                </Grid.Background>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>        
                <Button Command="{Binding ConnectCommand}" Grid.Column="0" Grid.Row="0" Content="Connect(d)" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Command="{Binding EditCommand}" Grid.Column="1" Grid.Row="0" Content="Edit(d)" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Command="{Binding DeleteCommand}" Grid.Column="2" Grid.Row="0" Content="Delete(d)" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <Grid Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Background="{StaticResource ConnectionCenterHostnameBackgroundColor}" Margin="0,0,0,0">
                    <TextBlock Text="{Binding Desktop.HostName}" Style="{StaticResource ConnectionCenterRegularTextStyle}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5,5,5,5"/>
                    <CheckBox 
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        IsChecked="{Binding Path=IsSelected, Mode=TwoWay}" />
                </Grid>            
            </Grid>
        </Grid>
    </StackPanel>
</UserControl>
