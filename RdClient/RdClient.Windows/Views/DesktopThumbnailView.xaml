<UserControl
    x:Class="RdClient.Views.DesktopThumbnailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <Button Command="{Binding ConnectCommand}" Content="Connect(d)" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        <Button Command="{Binding EditCommand}" Content="Edit(d)" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <Button Command="{Binding DeleteCommand}" Content="Delete(d)" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <TextBlock Text="{Binding Desktop.HostName}" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Calibri" FontSize="72" />

    </Grid>
</UserControl>
