<Page
    x:Class="RdClient.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:RdClient.Shared.ViewModels"
    mc:Ignorable="d"
>
    <Page.DataContext>
        <vm:SettingsViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="384" />
            <ColumnDefinition Width="2.5*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="{StaticResource SettingsNavBarBackgroundBrush}">
            <Button HorizontalAlignment="Left" VerticalAlignment="Top" Content="Back" Command="{Binding GoBackCommand}" />    
        </Grid>
        <Grid Grid.Column="1" Background="{StaticResource SettingsBackgroundBrush}">
            
        </Grid>
        
    </Grid>
</Page>
