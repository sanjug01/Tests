<controls:ModalUserControl
    x:Class="RdClient.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:RdClient.Views"
    xmlns:vm="using:RdClient.Shared.ViewModels"
    xmlns:controls="using:RdClient.Controls"
    xmlns:design="using:RdClient.DesignTime"    
    mc:Ignorable="d"    
    d:DesignWidth="500">
    
    <d:ModalUserControl.DataContext>
        <design:FakeSettingsViewModel />
    </d:ModalUserControl.DataContext>

    <controls:ModalUserControl.DataContext>
        <vm:SettingsViewModel />
    </controls:ModalUserControl.DataContext>

    <ContentControl Style="{StaticResource AccessoryViewStyle}">
		<StackPanel>
			<TextBlock 
				Margin="{StaticResource DialogItemVerticalSpacing}"
				x:Uid="Settings_NavBar_Title" 
				Text="Settings(d)" 
				Style="{ThemeResource TitleTextBlockStyle}">				
			</TextBlock>
			<controls:UserSettingsControl Margin="{StaticResource DialogSectionVerticalSpacing}"/>
            <controls:GatewaySettingsControl Margin="{StaticResource DialogSectionVerticalSpacing}"/>
            <controls:GeneralSettingsControl Margin="{StaticResource DialogItemVerticalSpacing}"/>
        </StackPanel>
    </ContentControl>
</controls:ModalUserControl>
