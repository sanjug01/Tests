<UserControl
    x:Class="RdClient.Controls.ConnectionCenterTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:RdClient.Controls"    
    xmlns:design="using:RdClient.DesignTime" 
    xmlns:converters="using:RdClient.Converters"
    xmlns:helpers="using:RdClient.Helpers"
    xmlns:shelpers="using:RdClient.Shared.Helpers"
    mc:Ignorable="d"
    d:DesignHeight="{StaticResource ConnectionCenterTitleBarHeight}"
    d:DesignWidth="1024">

    <d:UserControl.DataContext>
        <design:FakeConnectionCenterViewModel />
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <MenuFlyout x:Name="AddConnectionMenuFlyout">
                <MenuFlyoutItem 
                    x:Uid="Menu-AddDesktop"                 
                    Text="Add desktop(d)" 
                    Command="{Binding AddDesktopCommand}"/>
                <MenuFlyoutItem
                    x:Uid="Menu-AddWorkspace"
                    Text="Add workspace(d)"
                    Command="{Binding AddWorkspaceCommand}"/>                
            </MenuFlyout>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
            <converters:SegoeGlyphToSymbolIconConverter x:Key="segoeToSymbolIcon"/>
            <converters:ResourceIdToLocalizedStringConverter x:Key="ResourceIdToLocalizedStringConverter">
                <converters:ResourceIdToLocalizedStringConverter.LocalizedString>
                    <shelpers:LocalizedString/>
                </converters:ResourceIdToLocalizedStringConverter.LocalizedString>
            </converters:ResourceIdToLocalizedStringConverter>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource ConnectionCenterBarBackgroundBrush}" Height="{StaticResource ConnectionCenterTitleBarHeight}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <ListBox Style="{StaticResource ConnectionCenterSectionSelectorStyle}" SelectedIndex="0">
                <ListBoxItem 
                    x:Uid="ConnectionCenter_DesktopLabel"
                    IsSelected="{Binding Path=ShowDesktops, Mode=TwoWay}"
                    Visibility="{Binding Path=HasDesktops, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Content="Desktops(d)"/>
                <ListBoxItem
                    x:Uid="ConnectionCenter_AppLabel"
                    IsSelected="{Binding Path=ShowApps, Mode=TwoWay}"
                    Visibility="{Binding Path=HasApps, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Content="Apps(d)" />
            </ListBox>
        </Grid>
        <Button Grid.Column="2" Style="{StaticResource ConnectionCenterBarButtonStyle}" Content="{StaticResource AddIcon}" Flyout="{StaticResource AddConnectionMenuFlyout}" />
        <Button Grid.Column="3" Style="{StaticResource ConnectionCenterBarButtonStyle}" Content="{StaticResource MultiSelectIcon}" Command="{Binding ToggleDesktopSelectionCommand}" />


        <ItemsControl Grid.Column="5" ItemsSource="{Binding ToolbarItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplateSelector>
                <!--
                helpers:TypeNameDataTemplateSelector lists item templates keyed with the name of a bar item model class.
                x:Key of the <DataTemplate> elements may be one of the following:
                    › SegoeGlyphBarButtonModel - Glyph property may be converted to an icon by conv:SegoeGlyphToSymbolIconConverter.
                    › SeparatorBarItemModel - a vertical bar that may separate groups of buttons into logical groups
                -->
                <helpers:TypeNameDataTemplateSelector>
                    <helpers:TypeNameDataTemplateSelector.Templates>

                        <!-- SegoeGlyphBarButtonModel -->
                        <DataTemplate x:Key="SegoeGlyphBarButtonModel">
                            <Button
                                Style="{StaticResource ConnectionCenterBarButtonStyle}"
                                Command="{Binding Command}"
                                Content="{Binding Glyph, Converter={StaticResource segoeToSymbolIcon}}">
                            </Button>
                        </DataTemplate>

                        <!-- SeparatorBarItemModel -->
                        <DataTemplate x:Key="SeparatorBarItemModel">
                            <Rectangle Width="2" VerticalAlignment="Stretch"
                                               Fill="{ThemeResource AppBarSeparatorForegroundThemeBrush}"
                                               Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Margin="0, 8, 0, 12"/>

                        </DataTemplate>
                    </helpers:TypeNameDataTemplateSelector.Templates>
                </helpers:TypeNameDataTemplateSelector>
            </ItemsControl.ItemTemplateSelector>
        </ItemsControl>
    </Grid>
</UserControl>
