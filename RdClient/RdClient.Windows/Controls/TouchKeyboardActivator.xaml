<UserControl
    x:Class="RdClient.Controls.TouchKeyboardActivator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdClient.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="100"
    TabNavigation="Once">
    
    <Grid>
        <!-- Dummy disfunctional button that prevents the text box from gaining input focus automatically -->
        <Button x:Name="DummyButton" Opacity="0" TabIndex="1" />
        <!-- Hidden text box that gets input focus to show the touch keyboard -->
        <TextBox x:Name="HiddenTextBox"
                 Opacity="0"
                 IsTextPredictionEnabled="False"
                 IsSpellCheckEnabled="False"
                 BorderThickness="0"
                 BorderBrush="Transparent"
                 InputScope="Text"
                 TabIndex="2"/>
    </Grid>
</UserControl>
